[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "text-rpg-chatgpt"
version = "0.1.0"
description = "An open-source AI-assisted terminal text RPG."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["rpg_ai*"]
